#pragma once

#include <Eigen/Dense>

enum class rt_type
{
  X = 0,
  Y,
  D,
  S
};
enum class ft_type
{
  C2C,
  R2C
};

// typedefs
namespace types {

typedef unsigned int index_type;

}  // types
